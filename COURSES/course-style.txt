* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #eef2ff, #fdf2ff);
    color: #222;
    min-height: 100vh;
}

/* HEADER */

.course-header {
    background: linear-gradient(90deg, #4b0082, #0066ff);
    color: white;
    padding: 18px 7%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 4px 15px #999;
}

.logo {
    font-size: 23px;
    font-weight: bold;
}

.logo span {
    color: #ffe600;
}

.back-btn {
    text-decoration: none;
    color: #111;
    background: #ffe600;
    padding: 10px 20px;
    border-radius: 25px;
    font-weight: bold;
}

.back-btn:hover {
    background: white;
}

/* HERO */

.course-hero {
    text-align: center;
    padding: 70px 20px 50px;
    color: white;
    background: linear-gradient(135deg, #00c6ff, #7b2ff7);
}

.course-icon {
    font-size: 80px;
    margin-bottom: 15px;
}

.course-hero h1 {
    font-size: 48px;
}

.course-hero p {
    font-size: 20px;
    margin-top: 10px;
}

/* OPTIONS */

.course-options {
    max-width: 1100px;
    margin: 50px auto;
    padding: 0 25px;

    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 25px;
}

.option-card {
    background: white;
    padding: 35px 20px;
    border-radius: 20px;
    text-align: center;

    box-shadow: 0 7px 25px #ccc;

    transition: .3s;
}

.option-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 12px 30px #aaa;
}

.option-icon {
    font-size: 50px;
    margin-bottom: 15px;
}

.option-card h2 {
    color: #4b0082;
    margin-bottom: 12px;
}

.option-card p {
    color: #555;
    min-height: 55px;
}

.option-btn {
    display: inline-block;
    margin-top: 20px;
    padding: 11px 22px;

    text-decoration: none;
    color: white;

    background: linear-gradient(
        90deg,
        #4b0082,
        #0066ff
    );

    border-radius: 25px;
    font-weight: bold;
}

.option-btn:hover {
    background: linear-gradient(
        90deg,
        #ff0066,
        #ff9900
    );
}

/* INFORMATION */

.course-info {
    max-width: 1000px;
    margin: 30px auto 70px;
    padding: 40px 25px;

    background: white;
    border-radius: 25px;

    box-shadow: 0 5px 25px #ddd;
}

.course-info h2 {
    color: #4b0082;
    margin-bottom: 20px;
}

.course-info ul {
    padding-left: 25px;
}

.course-info li {
    margin: 10px 0;
}

/* FOOTER */

footer {
    background: #111;
    color: white;
    text-align: center;
    padding: 30px;
}

footer strong {
    color: #ffe600;
}

/* MOBILE */

@media(max-width:900px) {

    .course-options {
        grid-template-columns: repeat(2, 1fr);
    }

}

@media(max-width:600px) {

    .course-header {
        flex-direction: column;
        gap: 15px;
        text-align: center;
    }

    .course-hero h1 {
        font-size: 35px;
    }

    .course-options {
        grid-template-columns: 1fr;
        margin-top: 30px;
    }

    .course-icon {
        font-size: 60px;
    }

}
.quiz-layout{
    display:grid;
    grid-template-columns:
    minmax(0,1fr) 300px;
    gap:25px;
    align-items:start;
}